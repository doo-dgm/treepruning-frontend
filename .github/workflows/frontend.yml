name: Frontend CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

env:
  # Renombrado de tree-pruning-frontend -> treepruning-frontend para alinear
  # con el nombre del repo y crear un namespace GHCR limpio (el anterior
  # tenia 403 en cross-package blob mount que no se podia destrabar).
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/treepruning-frontend

jobs:

  build-and-push:
    name: Build & Push Docker image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ github.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name == 'push' }}
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:${{ github.ref == 'refs/heads/main' && 'latest' || 'dev' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Desactiva los attestations de provenance/SBOM. Con mode=max generan
          # blobs adicionales que GHCR intenta resolver via "cross-repo mount"
          # cuando ya hay capas compartidas en otros packages del owner. Si el
          # GITHUB_TOKEN no tiene permiso write sobre esos otros packages,
          # responde 403 al HEAD del blob y aborta el push.
          provenance: false
          sbom: false

  deploy-dev:
    name: Deploy a Dev
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'

    steps:
      - name: Disparar actualizacion en infra
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INFRA_DISPATCH_TOKEN }}
          repository: ${{ github.repository_owner }}/treepruning-infra
          event-type: update-frontend
          client-payload: |
            {
              "service": "frontend-dev",
              "sha": "${{ github.sha }}",
              "actor": "${{ github.actor }}"
            }

  deploy-prod:
    name: Deploy a Prod
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production

    steps:
      - name: Disparar actualizacion en infra
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INFRA_DISPATCH_TOKEN }}
          repository: ${{ github.repository_owner }}/treepruning-infra
          event-type: update-frontend
          client-payload: |
            {
              "service": "frontend",
              "sha": "${{ github.sha }}",
              "actor": "${{ github.actor }}"
            }
